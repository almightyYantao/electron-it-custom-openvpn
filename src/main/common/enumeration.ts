/**
 * 用户配置
 */
export enum USER {
  EXIT_AUTO_LOGIN = 'user.autoLogin',
  USER_TOKEN = 'user.token',
  USER_USERNAME = 'user.username',
  USER_PASSWORD = 'user.password',
  USER_REMEMBER_ME = 'auto.remember'
}

/**
 * 基础配置
 */
export enum BASE {
  APP_PATH = 'appPath',
  STATIC_PATH = 'staticPath',
  VERSION = 'version',
  LOCAL_LANGUAGE = 'localLanguage'
}

/**
 * 软件安装
 */
export enum INSTALL_APP {
  SUCCESS = 'success',
  ITEM = 'item',
  PERCENT = 'percent'
}

/**
 * VPN配置
 */
export enum VPN_ENUM {
  CONNECT_STATUS = 'connectStatus',
  CONNECT_STATUS_STATUS = 'connectStatus.status',
  CONNECT_STATUS_CONNECTING = 'connectStatus.connecting',
  INT_VERSION = 'init.version',
  VPN_PORT = 'vpnPort',
  CONFIG_VALUE = 'configValue',
  SECRET_KEY = 'H!o18u$@%^',
  SECRET_KEY_IV = 'H1u@@#*U(*'
}
